<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity" android:id="@+id/viewRoot"
        android:background="@drawable/background">
    <androidx.recyclerview.widget.RecyclerView
            android:layout_width="413dp"
            android:layout_height="648dp" android:layout_marginTop="1dp"
            android:scrollbars="vertical"
            app:layout_constraintTop_toBottomOf="@+id/supSearch" app:layout_constraintStart_toStartOf="parent"
            android:id="@+id/supList"/>
    <SearchView
            app:queryHint="Ej: Batman"
            android:layout_width="409dp"
            android:layout_height="81dp"
            android:id="@+id/supSearch" app:layout_constraintTop_toTopOf="parent" android:layout_marginTop="1dp"
            app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="1dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>